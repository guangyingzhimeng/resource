# Karpathy 又出「神作」！一个 630 行的 Python 脚本，让 AI 自己搞科研

你可能已经听说了 Karpathy 的新项目 **AutoResearch**。

如果只看到"让 AI 自动跑实验"这个表面，那确实有点意思。但说实话，大部分人没看懂背后那套方法论——它其实可以应用到任何领域。

我花了点时间拆解这个项目，发现它提供了一套**通用的实验思维框架**。不只是搞机器学习的能用，做产品的、做营销的、甚至优化团队会议都能用上。

---

## 01 这个项目到底在做什么？

Karpathy 把之前研究的 nanochat LLM 训练核心，简化成了**一个 ~630 行的单文件 Python 脚本**，单 GPU 就能跑。

核心就两件事：

- **人类**负责迭代 prompt（写什么目标、什么规则）
- **AI 代理**负责迭代训练代码（怎么优化模型）

每次实验**严格跑 5 分钟**，然后看结果：验证集 loss 降低了就保留，升高了就回滚。

然后 AI 会基于这次的结果，继续尝试下一个改动。日积月累，一晚上就能跑 100 多次实验。

Karpathy 用了 2 天时间让 AutoResearch 自动优化 nanochat 模型，**发现了约 20 个提升验证集 loss 的改进**，而且这些改进还能迁移到更大的模型（depth=24）上。

---

## 02 五大核心方法论，任何领域都能用

深入拆解后，我发现了这套系统的核心方法论：

### 1. 让专家定标准，让系统去试错

传统做实验的方式是人类自己上：设计实验、执行、分析结果、决定下一步。

Karpathy 的方案是倒过来的——**人类只负责制定"什么是好"的标准，剩下的让系统自动完成**。

这在 AutoResearch 里体现为：
- `program.md`：人类写的规则（什么是好、什么是坏）
- `train.py`：AI 改动的代码

但这个思路放到其他领域一样好用：

- 做广告的可以让设计师定义"好广告 = 点击率 > 5%，且符合品牌调性"，然后让系统批量生成 100 个标题+图片组合去测试。
- 做产品的可以定义"好设计 = 用户完成率 > 80%，步骤不超过 3 个"，然后自动调整界面布局做 A/B 测试。

**关键是把"判断权"和"执行权"分开。人类擅长定性判断，机器擅长大规模定量试错。**

---

### 2. 时间盒约束：不要最优，要够快

Karpathy 有个洞察挺有意思：**不要问"什么是最优解"，要问"在 5 分钟内能找到什么好解"。**

AutoResearch 每次实验严格跑 5 分钟就停。这不是抠门，而是一种设计选择——**约束会逼你找到更聪明的捷径**。

不同类型的约束适用于不同场景：

- **时间盒**（5 分钟、1 小时、1 天）：适合需要快速迭代的领域
- **预算盒**（100 元、1000 元）：适合商业实验
- **数量盒**（10 个版本、100 次尝试）：适合创意生成
- **样本盒**（100 个用户、1 个区域）：适合市场测试

对比一下两种思路：

- 传统做法：**"我们花 3 个月做个完美版本再发布"**——风险在于做完了可能发现没人要
- AutoResearch 思路：**"我们用 1 周做 10 个粗糙版本，每个测 100 个用户"**——快速找到方向，失败成本极低

---

### 3. 找到你的"北极星指标"

AutoResearch 用 `val_bpb`（验证集 bits per byte）作为**单一指标**。这个数字小了就保留，大了就回滚。没有讨论，没有纠结。

复杂世界被压缩成单一数字，**决策速度指数级提升**。

这听起来过于简化，但实际操作中极其有效。关键是找到那个真正重要的指标：

- 写作领域：别纠结"深刻、有趣、易懂"，直接看 **完读率 × 分享率**
- 招聘领域：别空谈"能力强、文化匹配、有潜力"，直接看 **试用期内绩效评分**
- 选址："人流大、租金低、竞争少"三难全，不如直接用 **月营收 / 月租金**
- 营销：别纠结"有创意、有传播、有转化"，直接算 **ROI**

好指标有三个特征：**能测量、当天或实时知道结果、单一不需要权衡**。

---

### 4. 实验也需要"后悔药"

AutoResearch 用 **Git 分支管理实验**。尝试 A 失败了就丢弃，尝试 B 成功了就合并到主干，然后基于 B 继续尝试 C。

这听起来是技术细节，其实是一种思维方式：**任何尝试都要有"回退"机制**。

不会用 Git 的人也能实践这个思路：

- 写文档的可以用版本历史，或者养成"另存为 v1、v2、v3"的习惯
- 做设计的可以用 Figma 的版本历史
- 定商业策略可以写决策日志，限定可撤销的试点范围
- 培养个人习惯可以做 30 天试验 + 日记记录，不行就换

**关键是不要因为害怕失败而不敢尝试——反正随时可以回到上一好状态。**

---

### 5. 设计"不用人在场"的系统

`program.md` 里有一句指令很有意思：

> "NEVER STOP... The human might be asleep"

核心理念是**把"人必须在场"变成"人可以不在场"**。

这需要设计一个完整的自主循环：

- **触发器**——什么情况下开始一轮实验？（时间驱动、事件驱动、条件驱动）
- **执行器**——具体做什么？要有明确的动作清单
- **判断器**——怎么算成功？怎么算失败？
- **记录器**——每次尝试都要记，方便事后复盘

这套机制搭好了，你就可以去睡觉，让系统自己跑。

---

## 03 实战：用这个框架优化团队会议

光说理论没意思，看个实际例子。

假设你们团队每周例会太浪费时间，想优化。很多人不知道怎么下手，或者用"感觉"试错。

用 AutoResearch 框架这么干：

1. **先定义实验空间**——哪些东西可以调整？比如会议时长（30/45/60 分钟）、参与人数、议程结构
2. **明确固定约束**——什么不能动？比如每周一次周三下午、必须有会议记录
3. **找到北极星指标**——怎么算"好"？可以用会议满意度评分（1-5 星）和决策效率（议题闭环率）
4. **设定资源盒**——打算投入多少资源试错？比如 4 周试验期、每周试 1 种新形式
5. **最后决策规则**——什么时候保留新形式？比如满意度 ≥ 4.0 且闭环率 ≥ 80%

跑 4 周下来，你会有一个**明确的"最佳会议形式"**，而且是数据支撑的，不是拍脑袋定的。

---

## 04 几个关键的心智转变

这套框架要求你改变一些固有观念：

- 从"**我要找到最优解**"变成"**我要在有限时间内找到足够好的解**"
- 从"**人必须参与每一步**"变成"**人监督，系统执行**"
- 从"**失败是坏事**"变成"**失败是数据，快速失败是优势**"
- 从"**专家直觉判断**"变成"**指标驱动决策**"
- 从"**做完美再发布**"变成"**快速试验，快速学习**"

---

## 05 一句话总结

AutoResearch 的本质是：**用"可计算的成功标准 + 严格的资源约束 + 版本化的试错机制"，把探索优化过程从"专家的手工艺术"变成"可自动化的大规模实验"。**

这套思想不只适用于机器学习。

**广告创意测试、产品界面优化、投资策略回测、教育内容设计、组织流程改进、个人习惯养成**——只要满足三个条件都能用：

1. **变量能编码**（可以写在配置里）
2. **结果能量化**（可以 return 一个数字）
3. **实验能快速验证**（5 分钟到 1 小时见分晓）

---

如果你对 AutoResearch 感兴趣，可以去 GitHub 查看完整代码（需要开源地址可公众号回复）。

研究这类的项目，关键不是复制代码，而是**理解背后的思维方式**。当你学会用"约束+指标+迭代"的视角看待问题时，很多看似复杂的问题都会变得可解。

---

*本文基于 Karpathy 的 AutoResearch 项目分析*

*首发于公众号：光影织梦*